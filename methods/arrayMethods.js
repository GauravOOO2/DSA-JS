const arr = [1,2,3,"chadd",55]
arr.push(4) // adds element into the array at the end
arr.unshift(0) // adds element at the start 
arr.pop()// removes element from last of the array
arr.shift() // removes element form the biginning of the array
for(const item of arr){
    console.log(item);
}


// learn about map , reduce, filter, concat, slice and splice